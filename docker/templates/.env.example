# Example environment file for Docker Compose projects
# Copy this file to .env and customize for your project
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your values
#   docker compose up -d

# Project settings
PROJECT_NAME=myproject
COMPOSE_PROJECT_NAME=${PROJECT_NAME}

# Application settings
APP_ENV=development
APP_PORT=8000
DEBUG=true

# PostgreSQL settings
POSTGRES_DB=mydb
POSTGRES_USER=myuser
POSTGRES_PASSWORD=changeme
POSTGRES_PORT=5432

# Redis settings (if applicable)
REDIS_PORT=6379
REDIS_PASSWORD=

# Web server settings (if using Nginx)
HTTP_PORT=80
HTTPS_PORT=443
DOMAIN=localhost

# pgAdmin settings (if using)
PGADMIN_EMAIL=admin@localhost
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# Security note:
# - Never commit .env files with real credentials to version control
# - Add .env to .gitignore
# - Use strong passwords for production
# - Consider using Docker secrets for sensitive data
