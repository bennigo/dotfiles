---
description: Architecture design — high-level decisions, tradeoffs, technology selection.
---
Use the subagent tool with a single "architect" agent:

Task: Design the architecture for {{focus}}. Present options with tradeoffs, make decisions with rationale, produce ADR templates, API sketches, and data model designs. Flag open questions that need user input.

Output must be directly usable by planner → worker chain.
